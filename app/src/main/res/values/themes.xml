<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Financegamemoblile" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Financegamemoblile" parent="Base.Theme.Financegamemoblile" />

    <!-- Dark theme for the game -->
    <style name="Theme.FinanceGame.Dark" parent="Theme.Material3.Dark.NoActionBar">
        <item name="colorPrimary">@color/morado</item>
        <item name="colorOnPrimary">@color/blue1</item>
        <item name="android:windowBackground">@color/blue1</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="Theme.FinanceGame.Exercise" parent="Theme.FinanceGame.Dark">
        <item name="android:radioButtonStyle">@style/Widget.FinanceGame.RadioButton</item>
    </style>

    <style name="Widget.FinanceGame.RadioButton" parent="android:Widget.CompoundButton.RadioButton">
        <item name="android:background">@drawable/radio_button_selector</item>
        <item name="android:button">@null</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:padding">16dp</item>
        <item name="android:layout_marginBottom">8dp</item>
        <item name="android:textAlignment">center</item>
    </style>

</resources>