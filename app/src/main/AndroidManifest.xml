<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Financegamemoblile"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".login.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".register.RegisterActivity"
            android:exported="false" />

        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:theme="@style/Theme.FinanceGame.Dark" />

        <activity
            android:name=".courses.CourseActivity"
            android:exported="false"
            android:label="Cursos"
            android:theme="@style/Theme.FinanceGame.Dark" />

        <activity
            android:name=".lessons.LessonActivity"
            android:exported="false"
            android:label="Lecciones" />

        <activity
            android:name=".exercises.ExerciseActivity"
            android:exported="false"
            android:label="Ejercicio"
            android:theme="@style/Theme.FinanceGame.Exercise" />

    </application>

</manifest>